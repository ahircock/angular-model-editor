import { TextControllerDirective } from './text-controller.directive';

describe('TextControllerDirective', () => {
  it('should create an instance', () => {
    const directive = new TextControllerDirective();
    expect(directive).toBeTruthy();
  });
});
